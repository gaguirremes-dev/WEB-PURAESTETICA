---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html
    - /bin/cp -R pura-estetica/. "$DEPLOYPATH/"
    - /bin/rm -f "$DEPLOYPATH/fix-servicios.js"
    - /bin/rm -f "$DEPLOYPATH/tailwind.config.js"
    - /bin/rm -f "$DEPLOYPATH/tailwind-input.css"
    - /bin/rm -f "$DEPLOYPATH/package.json"
    - /bin/rm -f "$DEPLOYPATH/package-lock.json"
    - /bin/rm -f "$DEPLOYPATH/cpanel.yml"
    - /bin/rm -f "$DEPLOYPATH/ASSETS_NEEDED.md"
    - /bin/rm -f "$DEPLOYPATH/README.md"
